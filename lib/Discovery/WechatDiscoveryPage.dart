import 'package:flutter/material.dart';

class WechatDiscoveryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:AppBar (
        title: Text("发现"),
        centerTitle: true,
      ),
      body: Center(
        child: Text("这是发现页，待开发"),
      ),
    );
  }
}